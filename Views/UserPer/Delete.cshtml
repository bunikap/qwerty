@model qwerty.Models.UserPer

@{
    ViewData["Title"] = "Delete";
}
@*
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
    <h4>UserPer</h4>
    <hr />
    <dl class="row">
    <dt class="col-sm-2">
    @Html.DisplayNameFor(model => model.Ownerss)
    </dt>
    <dd class="col-sm-10">
    @Html.DisplayFor(model => model.Ownerss.Id)
    </dd class>
    <dt class="col-sm-2">
    @Html.DisplayNameFor(model => model.Permissions)
    </dt>
    <dd class="col-sm-10">
    @foreach (var item in Model.AvailablePermission)
    {

    <div class="checkbox">
    <label>
    @item.Text
    </label>
    </div>
    }
    </dd class>
    </dl>

    <form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-action="Index">Back to List</a>
    </form>


    </div> *@


<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-md-6">
                <ol class="breadcrumb ">
                    <li class="breadcrumb-item"><a asp-area="" asp-controller="UserPer" asp-action="Index">User
                            Permission</a></li>
                    <li class="breadcrumb-item active">Delete</li>

                </ol>
            </div>
            <div class="col-md-6">
            </div>
        </div>
    </div>
</section>



<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Delete User Permission</h3>
                    </div>
                    <div class="card-body">
                        <dl class="row">
                            <dt class="col-sm-2">
                                <label>User</label>
                            </dt>
                            <dd class="col-sm-10">
                                @Html.DisplayFor(model => model.Ownerss.own)
                            </dd class>
                            <dt class="col-sm-2">
                                <label>Permission</label>
                            </dt>
                            <dd class="col-sm-10">
                                @foreach (var item in Model.AvailablePermission)
                                {

                                    <div class="checkbox">
                                        @* <label> *@
                                        <li>@item.Text</li>
                                        @* </label> *@
                                    </div>
                                }
                            </dd class>
                        </dl>
                    </div>
                    <div class="card-footer">
                        <form asp-action="Delete">
                            <button type="button" class="btn btn-secondary"
                                onclick="location.href='@Url.Action("Index", "UserPer")'">Back</button>
                            <input type="hidden" asp-for="Id" />
                            <input type="submit" value="Delete" class="btn btn-danger" />
                            
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>